# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  name: Default

steps:
- task: AzureKeyVaultt@1
  displayName: 'Azure Key Vault: kuvsf18a4d'
  inputs:
    azureSubscription: rmd
    KeyVaultName: kuvsf18a4d
